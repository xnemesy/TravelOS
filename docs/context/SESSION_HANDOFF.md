# SESSION_HANDOFF.md

> Documento vivo — da aggiornare a fine di ogni sessione di lavoro significativa (umana o con agente AI), non solo periodicamente. Obiettivo: chiunque riprenda il progetto dopo giorni o settimane deve poter leggere solo questo file e sapere esattamente dove riprendere, senza dover ricostruire il contesto da `git log`. Se questo file non viene aggiornato, il primo segnale è che la sessione successiva lo scoprirà disallineato — trattalo con la stessa disciplina di [DECISIONS.md](DECISIONS.md) rispetto allo stato reale delle ADR.

---

## Ultimo aggiornamento: 2026-07-09 (fine sessione, sesta parte)

## Ultimo sprint (2026-07-09, sesta parte) — ADR-018: refinement da review architetturale indipendente

Una review indipendente (Gemini) su ADR-018 ha sollevato 2 punti bloccanti, 2 "should fix", 1 "nice to have". Ciascuno valutato indipendentemente prima di agire (nessuna modifica al codice fino al consenso esplicito) — verdetto per punto:
- **Concordato e applicato**: prerequisito Planner troppo rigido per i day-trip — verificato che `Trip`/`TripCalculator.getDuration()` già supportano trip a 0 notti, la regola originale li avrebbe bloccati ingiustamente. Corretto: `accommodations` è ora un prerequisito Planner solo se `tripDurationNights >= 1`.
- **Concordato parzialmente e applicato (con soluzione diversa da quella proposta)**: validazione incrociata date Setup/Trip mancante — la soluzione proposta (fondere `TripSetup` dentro `Trip`) è stata **scartata**: romperebbe il pattern satellite già stabilito da ADR-017 per `TravelPlace` (aggregato separato, referenziato via `tripId`, mai embedded), e in DDD classico la validazione cross-aggregato non richiede necessariamente di fondere gli aggregati. Documentata invece in ADR-018 §6 (solo testo, non implementata) una futura `validateSetupAgainstTrip(trip, setup): DateRangeViolation[]`.
- **Concordato parzialmente e applicato**: sequenzialità trasporti multi-tratta — aggiunto `Transport.sequenceOrder?: number` (opzionale, se assente l'ordine si deduce da `departureDate`); **respinta** la parte "link tra segmenti/grafo" come over-engineering senza consumer reale.
- **Non concordato, nessuna modifica**: record orfani alla cancellazione del Trip come motivo per fondere gli aggregati — verificato che il problema è **già presente, oggi**, per `PlacesEngine`/`TimelineEngine`/`TripLifecycleWatcher` (nessuno pulito da `deleteTrip()`), quindi sistemico e non specifico di `TripSetup`; "risolverlo" solo per Setup sarebbe incoerente. Consigliato (non eseguito, fuori scope) di tracciarlo come voce trasversale in `KNOWN_DEBT.md` in una sessione dedicata.
- **Non concordato, nessuna modifica**: tassonomia condivisa `interests`/`PlaceCategorySchema` per il futuro AI Concierge — verificato in ADR-014 che `TasteProfile` deriva le proprie affinità di categoria da segnali **osservati** (eventi reali), non da preferenze **dichiarate** — accoppiarle ora confonderebbe una distinzione che ADR-014 tiene deliberatamente separata; inoltre l'AI Concierge non ha ancora una propria ADR (Sprint 19).

**Consegnato**:
- ADR-018 §3.1 (nuovo campo `sequenceOrder?`), §4 (firma `evaluate(setup, tripDurationNights)`), §5 (riscritta: prerequisito `accommodations` condizionale), §6 (nuova nota Future Work, solo testo) aggiornate.
- `trip-setup.model.ts`: `TransportSchema` ora accetta `sequenceOrder?: number`.
- `SetupCompletionEngine.ts`: `evaluate()` accetta `tripDurationNights: number` esplicito (non un `Trip` — resta un Domain Service puro); `accommodations` richiesto per lo sblocco Planner solo se `tripDurationNights >= 1`, `transports` sempre richiesto.
- Test aggiornati/aggiunti: 6 nuovi (2 su `sequenceOrder`, 4 sullo scenario day-trip) + tutti gli 8 test esistenti dell'engine aggiornati alla nuova firma.
- **Verificato**: `tsc --noEmit` invariato (2 errori preesistenti, non correlati); `jest` 111/111 verdi (105 + 6 nuovi).
- **Non toccato, come richiesto**: nessuna fusione di `TripSetup` in `Trip`, nessuna modifica a `Trip`, nessuna persistenza/repository, nessun concetto AI.

## Sessione precedente (2026-07-09, quinta parte) — ADR-018: Trip Setup Domain & SetupCompletionEngine

Richiesta esplicita: "Implementa ADR-018 – Trip Setup Engine", con lettura preliminare di ADR-015/ADR-017/CURRENT_ROADMAP.md/SESSION_HANDOFF.md/PRODUCT_PRINCIPLES.md richiesta esplicitamente. Verificato prima di scrivere codice: **ADR-018 non esisteva** (zero file, zero menzioni di "Trip Setup"/"SetupCompletion" in tutto il repository) — a differenza di Sprint 14/15, qui non c'era una decisione pregressa da attivare. Coerente con `CURRENT_ROADMAP.md` ("Motori Fase 2/3... prima di implementarne uno, va scritta un'ADR che ne descriva scope e vincoli"), l'ADR è stata scritta **in questa stessa sessione**, non in una sessione di sola decisione separata — motivato esplicitamente in ADR-018 §0, perché non c'era nulla di già deciso da "non riscrivere" (principio "decisione ≠ attivazione", PRODUCT_PRINCIPLES.md §9, si applica solo a decisioni pregresse). Notato anche che PRODUCT_PRINCIPLES.md §9 cita "un'ADR-018 ridondante" come esempio ipotetico da evitare (per ADR-014) — verificato non essere una prenotazione del numero, solo un esempio; questa è una decisione diversa e genuina.

**Consegnato — rigorosamente solo dominio/engine/test, come richiesto (nessun wizard, React, screen, componente, AI, Planner)**:
- [ADR-018](../adr/018-trip-setup-engine.md) — decide 7 modelli di dominio e le regole minime di sblocco del Planner (decisione di prodotto dichiarata come tale, non presentata come necessità tecnica: verificato che `JourneyComposer` oggi non legge alcun dato di Setup).
- [`trip-setup.model.ts`](../../src/domain/trip/models/trip-setup.model.ts) — `Transport`, `Accommodation`, `Mobility`, `TripConstraint`, `TripDocument`, `TripPreferences`, `TripSetup` (aggregato radice), tutti schemi Zod nello stile di `place.model.ts`. Distinti esplicitamente da `TripEventSchema` (esistente, entrata di calendario leggera — non toccata, non sostituita). Regola di dominio: `undefined` (sezione mai affrontata) ≠ array vuoto/oggetto presente (affrontata, esplicitamente vuota).
- [`SetupCompletionEngine.ts`](../../src/domain/trip/engine/SetupCompletionEngine.ts) — Domain Service puro (nessun I/O, nessuna dipendenza da orologio/random, coerente con PRODUCT_PRINCIPLES.md §8), un solo metodo `evaluate(setup): SetupCompletionReport` → percentuale (6 sezioni, peso uguale), sezioni completate/mancanti, `plannerReadiness` con soglia dedicata **più stretta**: `accommodations`+`transports` devono avere almeno un elemento (non basta "affrontata" con array vuoto) — le due metriche (percentuale vs sblocco Planner) possono legittimamente divergere, per costruzione.
- 28 nuovi test (20 su schemi Zod — invarianti come `checkOut > checkIn`, `arrivalDate >= departureDate`, `modes` non vuoto — 8 sull'engine, inclusi determinismo e non-mutazione dell'input).
- **Verificato**: `tsc --noEmit` invariato (2 errori preesistenti, non correlati, invariati da Sprint 14); `jest` 105/105 verdi (77 + 28 nuovi).
- **Deliberatamente non implementato** (vincolo esplicito della richiesta): nessuna persistenza (repository/store/MMKV), nessun wizard UI, nessun collegamento a `JourneyComposer`/Planner — tracciato come prossimo passo non deciso in ADR-018 §7.

## Sessione precedente (2026-07-09, quarta parte) — fix bug produzione: categoria provider non validata

Prima esecuzione reale su dispositivo (Expo Go) della pipeline ADR-017/Sprint 14-15. Log runtime mostrava `PlaceMergeEngine.mergeFromProvider: TravelPlace costruito non valido — category: Invalid option` per un risultato di ricerca live. Diagnosi: `externalFromMetadata()` ([PlaceMergeEngine.ts](../../src/domain/trip/engine/PlaceMergeEngine.ts)) faceva `category: meta.category as any` — cast cieco, mai validato. I provider reali (`real-places.adapter.ts`, `mock-travel.providers.ts`) emettono `'attraction'` (il valore più comune del dataset curato, ~20 occorrenze), `'food'`, `'cultural'`, `'nightlife'` — nessuno dei quattro è tra i 10 valori di `PlaceCategorySchema`. Il bug era latente da Sprint 14 Fase 1 (`externalFromMetadata` esisteva già) ma invisibile finché `mergeFromProvider`'s ramo "create" (validato con Zod, a differenza di `mergePlace` che non valida mai) non ha avuto un chiamante reale — cioè fino a Sprint 14 Fase 2/Sprint 15 di questa stessa giornata.

**Consegnato**: `PlaceMergeEngine.normalizeCategory()` — mappa i valori provider noti (`attraction→landmark`, `food→restaurant`, `cultural→museum`, `nightlife→bar`), lascia invariati i valori già validi, ricade su `'other'` per qualunque valore sconosciuto (mai un errore). Usato da `externalFromMetadata`, quindi corregge sia il ramo nuovo (`mergeFromProvider`, prima rotto) sia il ramo esistente (`mergePlace`, che prima passava silenziosamente categorie invalide senza mai accorgersene, non validando). 7 nuovi test di regressione. **Verificato**: `tsc --noEmit` invariato (2 preesistenti); `jest` 77/77 verdi (70 + 7 nuovi).

## Sessione precedente (2026-07-09, terza parte) — Sprint 15, ADR-015 §2.6 (13.2): Domain Lifecycle

Richiesta di sessione etichettata "Sprint 15 — Domain Lifecycle (ADR-015 Phase 13.2 + 13.3)" con scope che includeva `TripCreated`/`TripCancelled` e ometteva `PlaceRated` (la vera 13.3). Prima di scrivere codice, letti ADR-015/ADR-017/roadmap/handoff come richiesto, poi verificato nel codice che due punti dello scope non erano decisioni già prese: `TripCancelled` non ha alcun write path in tutto il repository (nessuna azione scrive mai `status: 'cancelled'` — stesso problema già risolto per `PlaceRated` in Sprint 13.1), `TripCreated` non è nel design di ADR-015 (che copre solo transizioni derivate dal tempo via watcher fire-once; `createTrip()` esiste già come azione reale e non ne ha bisogno). Fermato esplicitamente prima di implementare, per istruzione esplicita dell'utente ("stop immediately if another architectural decision becomes necessary") — tre domande poste, risposta dell'utente: escludere `TripCancelled` ed escludere `TripCreated` da questo sprint, etichettare come "Sprint 15 — ADR-015 §2.6 (13.2)" senza reclamare la 13.3.

**Consegnato (solo ADR-015 §2.6, come confermato)**:
- `TripLifecycleWatcher` ([trip-lifecycle.watcher.ts](../../src/core/engines/lifecycle/trip-lifecycle.watcher.ts)) — confronta lo stato derivato (`TripCalculator.getTripStatus`, invariato) con l'ultimo stato osservato/persistito (MMKV, chiave `trip_lifecycle_${tripId}`), pubblica `TripStarted`/`TripCompleted` (già riservati in `DomainFactType` da Sprint 13.1, mai pubblicati) solo alla prima transizione realmente osservata. Deterministico, idempotente (chiamate concorrenti sullo stesso trip serializzate con una coda in-memory per tripId), testato in isolamento con storage/eventBus finti (nessuna dipendenza da MMKV/AsyncStorage reali nei test — verificato che `MMKVAdapter` non è importabile sotto Jest in questo progetto, `AsyncStorage` nativo non mockato).
- Risolta la "decisione di design ancora aperta" segnalata da ADR-015 §2.6 (finestra di osservazione saltata): il watcher pubblica **in sequenza** un fatto per ogni tappa attraversata (`TripStarted` poi `TripCompleted`), mai un salto silenzioso.
- Aggiunti `TripStartedPayload`/`TripCompletedPayload` a `events.types.ts` (nessuna modifica a `DomainFactType`, già completo da Sprint 13.1).
- Cablato in `trip.store.ts:loadTrips()`, fire-and-forget — nessun cambio al contratto o ai tempi di `loadTrips()` verso la UI, nessun file `app/` toccato, `PlacesEngine`/`JourneyComposer`/`TimelineRuleEngine` non toccati (verificato con `git diff --name-only`).
- **Debito scoperto, non corretto (fuori scope)**: `TripCalculator.getTripStatus()` non ha un corto circuito per `status === 'archived'` (a differenza di `'cancelled'`) — un trip archiviato viene derivato con la stessa logica a date di un trip normale. Il watcher gestisce comunque `'archived'` correttamente nella sua mappatura interna (`toWatchedStage`), ma quel ramo è irraggiungibile passando per `TripCalculator` così com'è oggi. Testato direttamente, documentato, non corretto.
- **Verificato**: `tsc --noEmit` invariato (2 errori preesistenti, non correlati); `jest` 70/70 verdi (55 + 15 nuovi).

## Sessione precedente (2026-07-09, seconda parte) — Sprint 14, Fase 2: fix issue bloccante da review indipendente

Una review indipendente (Gemini) sulla Fase 2 appena consegnata ha trovato una regressione funzionale bloccante: rimuovere il bypass `placeRepository` da `[placeId].tsx` senza sostituto rompe la preview di un luogo **non ancora salvato**. Verificato prima di agire: esistono almeno due percorsi UI reali che aprono quello schermo con un luogo non salvato — `places/index.tsx:605` (catalogo editoriale, `item.id`) e `places/index.tsx:773` (ricerca live, `meta.placeId`) — non un caso raro. Gli altri 4 punti "should fix" della stessa review (notes/isVisited/role/anchorType/durationMinutes mancanti in `canonicalPlaceToPlaceRef`) sono stati analizzati e classificati come reali ma **non contraddicono ADR-017 §3.3** (l'ADR stessa non elenca questi campi tra le "Proprietà consentite" di `PlaceRef`) — richiedono un'estensione esplicita di ADR-017 prima di essere implementati, non affrontati in questa sessione.

**Consegnato (solo il fix bloccante)**:
- `PlaceMergeEngine.mergeFromProvider`: il ramo "create" ora accetta `PlaceMetadata | ExternalPlace` (stesso type-guard già in `mergePlace`), non solo `PlaceMetadata` — chiude il limite noto che impediva a `EditorialPlaceItem.baseData` (già `ExternalPlace`-shaped) di attraversare la pipeline. 1 nuovo test.
- Nuovo hook `usePlaceDetails(tripId, placeId)` in `src/shared/hooks/usePlaceDetails.ts` — unico punto dietro cui vive la pipeline transiente Provider/Editorial → `mergeFromProvider` → `canonicalPlaceToPlaceRef` → `PlaceRef`, **mai persistita** (nessuna chiamata a `savePlace`). Tre sorgenti in ordine: già salvato (invariato) → catalogo editoriale statico → provider via SIP (`TravelServices.places().getPlaceDetails`). Il `PlaceRef.id` transiente viene sovrascritto con l'id di richiesta (non l'id interno generato per il `TravelPlace` effimero) per continuità di navigazione — dettaglio nel doc-comment del file.
- `[placeId].tsx` aggiornato per usare `usePlaceDetails` al posto dello `useState`/`useEffect` locale — nessun import `PlaceMetadata`/`TravelPlace`/`ExternalPlace` in UI, ADR-017 §5 intatta.
- **Verificato**: `tsc --noEmit` invariato (2 errori preesistenti, stessi di prima); `jest` 54/54 verdi (53 + 1 nuovo). Manuale: nessun ambiente di preview disponibile per questo progetto (Expo nativo, `react-dom`/`react-native-web` non installati — non aggiunti per non introdurre una dipendenza fuori scope) — verificato con trace statico del codice per i tre casi (salvato, ricerca live, catalogo editoriale): namespace id disgiunti tra le tre fonti (`real-*`/`mock-*` provider vs `edit-*` catalogo, verificato via grep), tutte le 19 voci del catalogo editoriale hanno `location.coordinates` (verificato), branch del type-guard in `mergeFromProvider` selezionato correttamente per entrambe le forme di input. **Limite dichiarato esplicitamente**: questo non sostituisce un test end-to-end su dispositivo/simulatore.
- Aggiornato ADR-017 §6 (piano di migrazione, righe #2, #5, #11) per riflettere il fix. Non Sprint 15 — restato in Sprint 14.

## Sessione precedente (2026-07-09, prima parte) — Sprint 14, Fase 2: prima adozione live di ADR-017

Richiesta di sessione arrivata etichettata "Sprint 15" con due microfasi (A: `canonicalPlaceToPlaceRef` + test, zero adozione; B: adozione in `[placeId].tsx`, eliminazione bypass `placeRepository`, eliminazione `core/domain/models/Place`). Prima di scrivere codice, verificato contro `CURRENT_ROADMAP.md`/ADR-017 che questo lavoro corrisponde esattamente a Sprint 14 Fase 2 (adozione ADR-017, passi #3/#5 del piano di migrazione), non a Sprint 15 (Domain Lifecycle/User Signals, ADR-015, contenuto non correlato) — numerazione confermata con l'utente prima di procedere, roadmap non risequenziata.

**Consegnato**:
- `canonicalPlaceToPlaceRef(place, schedulingContext?)` in `src/core/engines/mappers/CanonicalPlaceToPlaceRef.ts` (piano di migrazione passo #3) — copia solo il sottoinsieme read-oriented dichiarato in ADR-017 §3.3 (`id`, `name`, `category`, `coordinates`, `coverImageUrl`, `address`, `rating`, `priority`) più i campi di scheduling passati esplicitamente dal chiamante; lancia un errore esplicito se mancano le coordinate (violazione dell'invariante "solo `mergeFromProvider` crea un `TravelPlace`"). 4 nuovi test. Zero adozione da un chiamante reale, per costruzione — il collegamento a `TimelineEngine`/`JourneyComposer` (passi #8-#10) resta fuori scope.
- Rimosso il bypass `placeRepository.getPlaceDetails()` da `app/trip/[id]/places/[placeId].tsx` (piano di migrazione passo #5): il luogo si legge solo tramite `usePlaces(tripId)`; assente tra i salvati → stato vuoto già esistente in UI. Rimossi gli import `Place`/`PlaceCategory` da `core/domain` e lo stato tipato `Place` — ogni campo che il componente leggeva (`category`, `isVisited`, `notes`, `coverImageUrl`, `durationMinutes`, `address`) era già di fatto un `PlaceRef` a runtime, mai un `Place`: la causa esatta dei 24 errori tsc era solo l'annotazione di tipo, non la logica.
- **Verificato**: `tsc --noEmit` 26 → 2 errori (entrambi preesistenti, invariati, fuori dal percorso toccato — `itinerary.tsx:501`, `places/index.tsx:619`); `jest` 53/53 verdi (49 preesistenti + 4 nuovi).

**Scope deliberatamente tagliato, decisione presa con l'utente in sessione**: `src/core/domain/models/Place.ts` **non eliminato**. Motivo verificato nel codice prima di agire: è ancora importato da `PlaceRepository.ts`/`MockPlaceRepository.ts`/`TravelBackendRepository.ts`/`TravelBackendService.ts`, l'intera catena dietro il singleton `placeRepository` che `TravelServices.places()` usa oggi come **unico percorso reale verso il backend Cloud Run** (`.env`: `EXPO_PUBLIC_USE_REAL_PLACES=true`, endpoint di produzione configurato) — non un residuo morto, una connessione reale attiva. Eliminare il modello ora avrebbe richiesto eliminare anche quella catena, perdendo la connettività backend reale senza sostituto: ADR-017 §6 (piano di migrazione, passo #14) segnala esplicitamente questa dipendenza dal passo #4 (un vero `GooglePlacesAdapter` registrato nel SIP), non ancora costruito. Rinviato, tracciato in `CURRENT_ROADMAP.md`/`KNOWN_DEBT.md`, non un'omissione silenziosa.

## Sessione precedente (2026-07-08) — Sprint 14, Fase 1: primo codice reale su ADR-017, con review indipendente

Prima sessione con modifiche di codice da quando ADR-017 è stata decisa (sessione precedente, stesso giorno — vedi sotto). Scope deliberatamente ristretto, concordato in un piano approvato prima di toccare qualunque file (vedi `/Users/roccosantonastasio/.claude/plans/zazzy-kindling-wozniak.md`): solo dominio Place (`src/domain/trip/`), zero UI, zero `JourneyComposer`, zero `TimelineRuleEngine`, zero adozione in un percorso live — nessun cambiamento funzionale per costruzione. Consegna iniziale seguita, nella stessa sessione, da una review indipendente ("dimentica il piano, valuta solo il codice risultante") che ha trovato 2 difetti bloccanti e 5 osservazioni minori — tutti risolti prima del commit.

**Consegnato (stato finale)**:
- `PlaceMergeEngine.mergeFromProvider()` — punto unico di conversione Provider→Canonical Place (piano di migrazione ADR-017, passo #2). Delega a `mergePlace()` invariato per l'aggiornamento (dopo validazione `existing.tripId === options.tripId`, throw se incoerente); logica nuova per la creazione (prima assente), con un `id` generato indipendentemente da `externalProviderId` (convenzione già in uso in `TripRepository.createTrip`). Zero adozione verificata — solo la propria definizione e il proprio test lo referenziano.
- `external`/`personal` (gli alias duplicati di `baseData`/`memories` in `TravelPlaceSchema`) **rimossi fisicamente** dallo schema — non solo deprecati: la deprecazione in due passi pianificata inizialmente è stata scartata in review perché lasciava una finestra di stato incoerente (`baseData` aggiornato, `external` stantio, se un percorso dimenticato lo avesse mai valorizzato). Verificato zero uso in tutto il repository prima di rimuovere.
- **Correzione a ADR-017** (non una nuova decisione, una correzione di dettaglio verificato durante l'implementazione): i nomi canonici sono `baseData`/`memories`, non `external`/`personal` come scritto originariamente — l'uso reale nel repository (5 file su `baseData`, zero su `external`; zero su entrambi `memories`/`personal`) rende la scelta opposta a costo di migrazione molto più basso, a parità di decisione di fondo (un solo nome canonico per livello).
- **Due difetti bloccanti trovati e corretti dalla review**: (1) la prima versione faceva coincidere `id` con `incoming.placeId` (l'id del provider), contraddicendo la separazione `id`/`externalProviderId` già presente nel repository (`budapest.mock.ts`) e rischiando una collisione silenziosa in `InMemoryPlaceRepository` (Map globale chiavata su `id` tra tutti i trip) se lo stesso luogo fisico fosse salvato in due trip diversi; (2) il doc-comment della funzione dichiarava che la firma sarebbe rimasta stabile per adottare in futuro il catalogo editoriale come "secondo produttore" — falso, verificato: il catalogo produce dati `ExternalPlace`-shaped, non `PlaceMetadata`-shaped come richiesto dalla firma. Corretto il commento per dichiarare onestamente il limite invece di un contratto non mantenuto.
- 9 nuovi test (`PlaceMergeEngine.test.ts`, inclusi quelli che pinnano le due correzioni sopra), 49/49 verdi. `tsc --noEmit`: 26 errori preesistenti invariati (nessuno in scope), zero nuovi.
- **Debito scoperto**: `budapest.mock.ts` è un secondo punto di creazione di `TravelPlace` (dati mock statici scritti a mano), non riconciliato col nuovo punto unico — non è duplicazione di mapper (non trasforma dati provider), ma resta un punto di creazione parallelo, tracciato per una fase successiva. `mergeFromProvider` non è ancora compatibile con l'ingresso del catalogo editoriale (vedi difetto (2) sopra) — limite noto, non risolto in Fase 1.

## Sessione precedente, stesso giorno (2026-07-08) — decisione ADR-017

Sessione di sola analisi/decisione architetturale, come richiesto esplicitamente: nessuna riga di codice modificata. Prodotta [ADR-017](../adr/017-unificazione-modello-place.md) — la decisione che [CURRENT_ROADMAP.md](CURRENT_ROADMAP.md) indicava come prerequisito prima di toccare il modello Place.

**Correzioni rispetto all'ipotesi di partenza** (`Provider → Canonical Place → JourneyPlace → PlaceRef`), verificate direttamente nel codice, non assunte:
- `JourneyPlace` e il suo mapper (`mapPlaceToJourneyPlace`) sono **codice morto confermato** — zero chiamanti in tutto il repository. Rimossi dalla pipeline, non portati avanti come quarta tappa.
- "Provider" non è una sorgente singola: esistono **due percorsi indipendenti e mai riconciliati** per i dati esterni (`TravelServices.places()` → `PlaceMetadata`, sempre mock/curato; `placeRepository` singleton, Sprint 12, → `Place` core/domain, unico punto di contatto reale col backend, raggiunto solo come bypass in `[placeId].tsx`). Questo bypass è la causa diretta di 24 dei 26 errori `tsc` odierni (verificato con una run diretta di `npx tsc --noEmit`).
- `TravelPlace` (rivisto, senza gli alias duplicati `baseData`/`external` e `memories`/`personal`) è stato eletto **Canonical Place** — non deprecato. È l'unica rappresentazione con una vera separazione a tre livelli (External/Editorial/Personal) e garanzie strutturali reali (`PlaceMergeEngine`).

**Pipeline decisa**: `PlaceMetadata → TravelPlace → PlaceRef`, tre stadi, due mapper (`PlaceMergeEngine.mergeFromProvider` — standardizzazione di `mergePlace()` già esistente, non scritta da zero — e il nuovo `canonicalPlaceToPlaceRef`). `PlaceRef` smette di essere sia record di libreria persistito sia stato di pianificazione effimero (doppio ruolo verificato oggi in `PlacesEngine`): diventa solo proiezione, mai persistita per intero.

## Sprint precedente (2026-07-07)

Sessione in due parti nettamente distinte — prima documentazione, poi messa in sicurezza:

1. **Knowledge Base permanente** (commit `d77c0a7`): creati 26 documenti in `/docs` (vision, architecture, glossary, agents, context) basati su ispezione diretta del codice tramite 6 agenti di ricerca paralleli, più 4 documenti operativi (`ENGINE_MAP.md`, `DECISION_TREE.md`, `ENGINE_LIFECYCLE.md`, questo file), più `.claude/context/` con symlink ai documenti chiave.
2. **`.gitignore` — `.env` root non era ignorato** (commit `72bf7e7`): solo `.env*.local` era coperto; aggiunto `.env`/`.env.*`/`!.env.example`, a specchio del pattern già esistente per `backend/.env`.
3. **Scoperta critica durante la review della working tree**: quasi l'intero nucleo reattivo Fase 1 — l'intero `backend/`, `PlacesEngine`, il `DomainEventBus` stesso, `JourneyComposer`/Rule Engine, l'intero SIP (`TravelServices`), tutti i View Layer hook — **non era mai stato committato in nessun commit della storia del repo**. Alcuni test già committati (`PlaceMergeEngine.test.ts`, `DistanceCalculator.test.ts`) referenziavano sorgenti mai versionate: un fresh clone avrebbe fallito la build.
4. **Checkpoint di sicurezza** (commit `41bdfea`, pushato su `origin/main`): tutta l'implementazione reale (131 file: backend, engine, provider, hook, UI) committata in un unico commit non ancora suddiviso per storia pulita — deliberatamente, per azzerare subito il rischio di perdita prima di qualunque riorganizzazione. Verificato prima del push: backend `tsc --noEmit` → 0 errori; `npx jest` → 40/40 test passati, 4/4 suite verdi. App `tsc --noEmit` → **26 errori preesistenti**, non introdotti dal checkpoint (vedi sotto).

## Decisioni prese

- **La sicurezza del lavoro viene prima della pulizia della history.** Un unico commit checkpoint non suddiviso è stato scelto deliberatamente sopra uno split "pulito" in 9 commit logici (pianificato ma poi accantonato) perché il rischio di perdita era reale e immediato — la riorganizzazione della history resta un passo successivo, non urgente ora che il codice è al sicuro su `origin/main`.
- **I 26 errori TypeScript non vanno corretti riga per riga.** Sono la conferma misurata (compilatore, non solo lettura del codice) della frammentazione del modello Place già descritta come trade-off in [DOMAIN_MODEL.md](../architecture/DOMAIN_MODEL.md) — vanno risolti ragionando sulla pipeline del modello, non patchando i 26 punti singolarmente. Vedi la nuova sezione "Priorità immediata" in [CURRENT_ROADMAP.md](CURRENT_ROADMAP.md).
- **Riprioritizzazione**: la risoluzione del modello Place precede Sprint 13.2 — un modello frammentato rende più fragile qualunque nuovo motore costruito sopra (incluso il Domain Lifecycle watcher).
- Il "Provider Layer" di ADR-001 è considerato implementato (sotto il nome SIP/`TravelServices`), non più lavoro futuro — vedi [DECISIONS.md](DECISIONS.md#adr-001).
- I bug preesistenti scoperti durante un refactoring si documentano e si bloccano con un test, non si correggono silenziosamente (pattern ADR-016, ora eletto a principio di prodotto in [PRODUCT_PRINCIPLES.md](../vision/PRODUCT_PRINCIPLES.md#2-i-bug-si-documentano-non-si-nascondono)) — applicato oggi anche ai 26 errori tsc.
- Il percorso di pianificazione legacy (`PlannerEngine`/`usePlannerStore`) resta esplicitamente non esteso, in attesa di una decisione su rimozione o migrazione — vedi [KNOWN_DEBT.md](KNOWN_DEBT.md).
- La documentazione (`/docs`) è ora la fonte di verità per architettura/vision/decisioni; il codice resta fonte di verità per il comportamento effettivo — in caso di conflitto, verificare sempre nel codice (vedi [DECISIONS.md](DECISIONS.md#perché-lo-stato-dichiarato-in-unadr-può-non-bastare)).

## Problemi aperti

Vedi il dettaglio completo in [KNOWN_DEBT.md](KNOWN_DEBT.md). I più rilevanti per chi riprende il lavoro:
- **Frammentazione del modello Place** — la decisione è presa ([ADR-017](../adr/017-unificazione-modello-place.md)), **Fase 1+2 dell'implementazione fatte** (punto unico di conversione Provider→Canonical Place, mapper Canonical→PlaceRef, prima adozione live in `[placeId].tsx`, incluso il fix della regressione sui luoghi non salvati trovata in review indipendente — vedi hook `usePlaceDetails`). `tsc` 26 → **2 errori residui**, entrambi preesistenti e fuori scope (`itinerary.tsx:501`, `places/index.tsx:619` — non relativi al modello Place). `core/domain/models/Place.ts` e la catena `PlaceRepository`/`TravelBackendRepository`/`MockPlaceRepository`/`TravelBackendService`/`placeRepository` **restano intenzionalmente non toccati**: sono l'unico percorso reale verso il backend Cloud Run oggi (`.env`: `EXPO_PUBLIC_USE_REAL_PLACES=true`), e ADR-017 §6 (passo #14) ne vincola la rimozione al passo #4 (adapter reale nel SIP), non ancora costruito. Prossimo passo: passo #4, poi #14; separatamente, passi #6-#10 (adozione in `places/index.tsx`/`itinerary.tsx`/`PlacesEngine`/`TimelineEngine`, incluso il cambio di formato persistenza MMKV — il passo a rischio più alto del piano); passo #11 parzialmente sbloccato ma non adottato in scrittura. **Debito noto, non affrontato**: `canonicalPlaceToPlaceRef` non copre ancora `notes`/`isVisited`/`role`/`anchorType`/`durationMinutes` — reale ma non in contraddizione con ADR-017 §3.3 (che non li elenca), richiede un'estensione esplicita dell'ADR prima di essere implementato. Vedi [KNOWN_DEBT.md](KNOWN_DEBT.md#frammentazione-del-modello-place--ora-verificata-a-livello-di-compilatore-priorità-alta) (da aggiornare).
- History git non ancora riorganizzata: il commit `41bdfea` è un unico blob di 131 file, non split per feature — pulizia rimandata a dopo l'implementazione di ADR-017.
- Due percorsi di pianificazione paralleli (uno morto) — ADR-017 decide esplicitamente la rimozione del percorso legacy (`PlannerEngine`/`usePlannerStore`), non ancora eseguita.
- `IMemoriesEngine` ha un naming ambiguo rispetto al futuro Traveler DNA (ADR-014 raccomanda rename a `IRecapEngine`, non ancora fatto).
- `UserContext` (risoluzione reale `tripId → userId`) è un prerequisito bloccante per il Traveler DNA, non ancora costruito.
- Auth Firebase reale ma disconnessa dalla navigazione (`app/login.tsx`).
- `TripCalculator.getTripStatus()` non ha un corto circuito per `status === 'archived'` (a differenza di `'cancelled'`) — un trip archiviato viene derivato con la stessa logica a date di un trip normale. Scoperto durante Sprint 15, non corretto (fuori scope). Vedi [KNOWN_DEBT.md](KNOWN_DEBT.md#tripcalculatorgettripstatus-non-ha-un-corto-circuito-per-archived).

## Prossimo task

Roadmap di sprint aggiornata in [CURRENT_ROADMAP.md](CURRENT_ROADMAP.md) col principio "decisione ≠ attivazione" — ora [PRODUCT_PRINCIPLES.md §9](../vision/PRODUCT_PRINCIPLES.md#9-decisione--attivazione) — che evita di aprire ADR ridondanti solo per eseguire una decisione già presa:
1. **Sprint 14 — Implementare ADR-017** — 🟡 Fase 1+2 fatte (dominio + prima adozione live: `canonicalPlaceToPlaceRef`, bypass `placeRepository` rimosso da `[placeId].tsx`, tsc 26→2; hook `usePlaceDetails` per il caso luogo-non-salvato, trovato in review indipendente). **Prossimo (non ancora iniziato)**: passo #4 (adapter reale nel SIP) → passo #14 (ritiro `Place`/`placeRepository`/catena repository); indipendentemente, passi #6-#10 (adozione in `places/index.tsx`, `itinerary.tsx`, `PlacesEngine`, `TimelineEngine` — include la migrazione del formato di persistenza MMKV, rischio "Alto" nel piano ADR-017 §6); passo #11 (adozione in scrittura del catalogo editoriale, ora parzialmente sbloccato). **Se si vuole colmare il gap di campi in `canonicalPlaceToPlaceRef`** (`notes`, `isVisited`, `role`, `anchorType`, `durationMinutes`, emerso in review): richiede prima un'estensione esplicita di ADR-017 §3.3, non un'implementazione diretta.
2. **Sprint 15 — ADR-015 §2.6 (13.2)** — ✅ fatto: `TripLifecycleWatcher`, `TripStarted`/`TripCompleted`. **13.3 (User Signals, `PlaceRated`) non iniziata** — write path per `personalRating` ancora da costruire, scrive sul livello `personal` di `TravelPlace` (§3.2/§5.7 ADR-017) una volta fatta. `TripCreated`/`TripCancelled` deliberatamente esclusi da questo sprint (vedi sopra) — restano proposte future non decise da nessuna ADR.
3. **Sprint 16 — Attivare ADR-014, Fase A (Memory Capture)**: Event Bus → log episodico append-only, zero interpretazione. Bloccata da `UserContext`. Ora ha un momento discreto "il viaggio è finito" su cui basarsi (Sprint 15).
4. **Sprint 17 — Attivare ADR-014, Fase B (Memory Intelligence)**: TasteProfile, ProceduralProfile, decay temporale — dipende da Sprint 15 (fatto) e da una storia reale accumulata in Sprint 16.
5. **Sprint 18 — Context Assembly**: il Traveler DNA entra in `TravelContext` come segnale opzionale per `JourneyComposer`.
6. **Sprint 19 — AI Concierge**: verbalizza il contesto assemblato. **Richiede una nuova ADR dedicata** prima di partire — a differenza degli sprint 14-18, non è l'attivazione di una decisione già presa.
7. **Sprint 20 — Trip Setup Domain (ADR-018)** — ✅ fatto (solo dominio/engine/test). **Prossimo (non deciso da questa ADR, vedi §7)**: persistenza (`TripSetupRepository`/engine con storage MMKV, stesso pattern di `PlacesEngine`), wizard UI, decisione esplicita su se/come collegare la soglia di sblocco Planner a `JourneyComposer`.
8. **Pulizia della history git**: split del checkpoint `41bdfea` in commit logici, solo dopo i punti precedenti — non è più urgente, il lavoro è al sicuro su `origin/main`.

## Ultimo commit di questa sessione

Le modifiche di questa sessione (Sprint 14 Fase 2 + fix review indipendente, Sprint 15 ADR-015 §2.6, fix bug produzione categoria provider, Sprint 20/ADR-018 Trip Setup Domain — codice: `CanonicalPlaceToPlaceRef.ts`, `CanonicalPlaceToPlaceRef.test.ts`, `PlaceMergeEngine.ts`, `PlaceMergeEngine.test.ts`, `usePlaceDetails.ts`, `place-query.service.ts`, `src/shared/hooks/index.ts`, `app/trip/[id]/places/[placeId].tsx`, `events.types.ts`, `trip-lifecycle.watcher.ts`, `trip-lifecycle.watcher.test.ts`, `trip.store.ts`, `trip-setup.model.ts`, `trip-setup.model.test.ts`, `SetupCompletionEngine.ts`, `SetupCompletionEngine.test.ts`; documentazione: `ADR-017`, `ADR-018` (nuova), `CURRENT_ROADMAP.md`, `KNOWN_DEBT.md`, questo file) non sono ancora state committate al momento della stesura di questa sezione — verificato prima di procedere: `tsc --noEmit` → 2 errori preesistenti invariati (`itinerary.tsx:501`, `places/index.tsx:619`), zero nuovi; `jest` → 105/105 test verdi. Consulta `git log --oneline -1` per l'hash esatto una volta committato.

## ADR attive

Vedi indice completo con stato verificato in [DECISIONS.md](DECISIONS.md). In sintesi: ADR-001 (accettata, Provider Layer già implementato), ADR-014 (proposta, design-only — Sprint 15 ha ora costruito il suo prerequisito infrastrutturale), ADR-015 (proposta, Sprint 13.1 e §2.6/13.2 fatti — 13.3 User Signals non iniziata), ADR-016 (documentata, fix non implementato), **ADR-017 (proposta, decisione presa, Fase 1+2 dell'implementazione fatte — dominio + prima adozione live)** — la pipeline `PlaceMetadata → TravelPlace → PlaceRef` è la fonte di verità per qualunque lavoro sul modello Place; nomi canonici corretti durante l'implementazione da `external`/`personal` a `baseData`/`memories` (vedi ADR-017 §3.2); passo #14 (ritiro di `core/domain/models/Place` e catena repository) resta esplicitamente vincolato al passo #4 (adapter reale), non ancora eseguito. **ADR-018 (nuova, proposta — decisione e dominio/engine implementati nella stessa sessione)** — Trip Setup (`Transport`/`Accommodation`/`Mobility`/`TripConstraint`/`TripDocument`/`TripPreferences`/`TripSetup`) + `SetupCompletionEngine`; nessuna persistenza/wizard/collegamento Planner ancora decisi.
